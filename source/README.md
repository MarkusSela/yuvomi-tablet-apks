# Wrapper sources

`generic/` is the public generic wrapper with an initial screen for entering and saving the Yuvomi endpoint.

The personal Marco variant is kept locally and is intentionally not stored in this public repository because it contains a private endpoint.
